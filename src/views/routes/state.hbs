<div class="container p-4">
    {{#each resultado}}
        <div class="card">
            <div class="card-header">
                {{fullname}}
            </div>
            <div class="card-body">
            </div>
        </div>
    {{/each}}
</div>